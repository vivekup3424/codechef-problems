#include <bits/stdc++.h>
using namespace std;
#define MAX_LEN 150

int main()
{   int T;
    scanf("%d", &T);
    for (int i = 0; i < T; i++)
    {
        int N;
        scanf("%d",&N);
        int arr[MAX_LEN],m;
        arr[0] = 1;
        m = 1;
        for (int i = 1; i < N; i++)
        {
         for (int p = 0; p < m; p++)
         {
            arr[p]
         }
            
        }
        
    }    
    return 0;
}